# Server-only Gemini key. Never prefix this with VITE_ and never commit .env.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional. Defaults to gemini-1.5-flash if not set.
GEMINI_MODEL=gemini-1.5-flash
